{
  "name": "@yt-ddp/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core deduplication logic for YouTube playlists",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "oxlint .",
    "format": "oxfmt .",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}
