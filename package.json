{
  "name": "yt-playlist-dedupe-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --ui=tui",
    "build": "turbo run build --ui=tui",
    "test": "turbo run test --ui=tui",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "check": "turbo run check --ui=tui",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/bun": "1.3.12",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0",
    "turbo": "^2.9.6",
    "typescript": "latest"
  },
  "engines": {
    "bun": ">=1.3.9",
    "node": ">=18"
  },
  "packageManager": "bun@1.3.9"
}
